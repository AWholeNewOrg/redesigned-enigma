name: 'Redesigned Enigma'
description: 'Example Action'
inputs:
  greetee:
    description: 'Greetee'
    required: true
    default: 'World'
runs:
  using: "composite"
  steps:
    - run: |
        echo Hello ${{ inputs.greetee }}
        echo I am doing evil things
      shell: bash
